Data formatted into pivoted format from "FEATURES NORM TO V3 FINAL.csv", split by visit.
V3 data is not present because of how the data is normalized - as such it is only zeros.

The "unprocessed" datasets are issued from the same pivoting method, but without normalization to V3. 